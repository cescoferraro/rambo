export const routesMap = {
    HOME: {
        path: '/',
        thunk: (dispatch, getState) => { }
    },
    DASHBOARD: {
        path: '/whatever',
        thunk: (dispatch, getState) => { }
    },
    CAMERA: {
        path: '/camera',
        thunk: (dispatch, getState) => { }
    }
};
